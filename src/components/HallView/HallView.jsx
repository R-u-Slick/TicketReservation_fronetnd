import { Container, Typography } from "@material-ui/core";

const HallView = () => {
  return (
    <Container>
      <Typography variant="h4">Hall view</Typography>
    </Container>
  );
};

export default HallView;
